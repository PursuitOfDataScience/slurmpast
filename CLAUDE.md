# Working on slurmpast

Repository: `PursuitOfDataScience/slurmpast`.

## Reporting bugs: one issue per audit, not one per bug

When an audit, review, or ordinary piece of work turns up defects, **file them as a
single GitHub issue on the repository the code belongs to** — here,
`PursuitOfDataScience/slurmpast`.

**All the bugs found in one pass go in that one issue.** Do not open an issue per
finding. A ten-finding sweep is one issue with ten sections, ranked worst first. Ten
separate issues fragment a single review into a queue nobody reads in order, hide
which defects share a root cause, and lose the ranking that says what to fix first.

Applies whether the work was asked for as "find bugs", "review this", or was
incidental to another task.

### Confirm before you file

An issue is public and permanent, so a claim in one has to be right. Before a
finding goes in:

* **Reproduce it by running the code**, not by reading it. This repo settled on that
  in round three of `issues.md`: "each was reproduced by running the code before
  being written down." A finding you can only argue for is a hypothesis.
* **Re-verify the whole list once more before filing.** Findings that do not survive
  the second pass get **withdrawn or narrowed, and said so in the issue** — an audit
  that quietly drops its wrong claims is not one that can be trusted about the rest.
* **Check it is not already known.** Search the open issues, and read `issues.md`:
  every audit round is recorded there, including items decided "not a defect" or
  "by design" and the ones deliberately left **Open**. Read the current count out of
  the file rather than trusting a number written here — this line has already been
  stale once.
* **Check whether a test already pins the behaviour.** If a test asserts it, the
  behaviour is intended until argued otherwise — say so and argue it, rather than
  reporting it as an oversight. If no test covers it, say that too; this suite's
  recurring failure mode is "tests that assert less than they appear to."

### What each finding needs

* `file.py:line` for the site.
* The reproduction — the command or the few lines that trigger it, and its actual
  output. Paste real output, not a description of it.
* What the reader gets versus what they should get, and why it matters. Prefer the
  repo's own documented intent as the standard: much of this codebase explains in
  comments why a thing is done a certain way, and the strongest findings are the
  ones where the code has drifted from a rule it states about itself.
* Severity ordering across the issue, worst first.

State plainly what was verified and what was not, and keep code-defects, documentation
defects and minor nits in separate sections so a reader can act on the first without
wading through the last.

### Filing

* One issue, titled with the count and the headline defects, not "Bugs found".
* Say up front how the code was evaluated and whether the test suite passes, so a
  reader knows the findings are gaps rather than regressions.
* End every issue, comment and review body with the attribution footer:

  ```
  ---
  _Generated by [Claude Code](https://claude.ai/code)_
  ```

### After filing: stop there — the issue IS the deliverable

**Hard rule. File the issue and stop.** Do not create a branch, do not open a pull
request, do not touch code or tests. Not when the fix is one line, not when the
defect is plainly real, not when CI would prove it green. An audit that turns up
something serious does not promote itself into permission to change the code.

**Fix only when the request says to.** "Fix them", "work on the codebase",
"implement it", or a reply on the issue asking for the fix — those authorise it.
These do not, and none of them is a near miss:

* "check the codebase" / "check if everything is great now"
* "evaluate this" / "review this" / "is X broken?"
* "find any bugs" / "look for problems"

If a defect looks urgent enough that filing alone seems inadequate, say so in one
line at the end of the issue-filing turn and wait for an answer. One line, not a
branch.

This reverses what this file said for one round, and both directions have been asked
for at different times — which is precisely why it is written down instead of
inferred. The reversal came after a round that was asked "check the main codebase
thoroughly if everything is great now", filed the issue correctly, and then also
pushed a nine-file pull request nobody had requested. A wrong guess in this
direction is expensive in the reviewer's time, not the author's.

### When a fix HAS been asked for

* **Is it destructive or outward-facing?** Deleting data, force-pushing, publishing
  a release — ask even then. Editing code and tests — proceed.
* **Did a previous round decide this deliberately?** `issues.md` has an **Open**
  section for defects the maintainer chose not to fix and said why. Reopening one
  needs an argument that engages with the recorded reason, not a drive-by.
* Finish by recording the round in `issues.md` — what was fixed, what was withdrawn,
  what changes in the numbers a user might be depending on — and say plainly what is
  fully addressed and what is still open.

## Development

* **A branch exists only for work that was asked for.** See the rule above: an audit
  ends at the issue. Once a change *has* been asked for, it goes on a branch and
  lands through a PR — the branch named in the task if there is one, otherwise one
  you name. Do not commit to `main` directly; that is the maintainer's call, and it
  is how a change reaches CI before it reaches users.
* `ruff`, `ruff format` and `mypy` must stay clean (config in `pyproject.toml`). Run
  all four gates including `pytest` before pushing — `mypy` is the one that gets
  skipped locally and caught by CI.
* Tests live in `tests/`, run with `pytest`. A fix ships with a test and its control.
  Write the control deliberately: this suite's recurring failure mode is a test that
  pins the buggy value, and round five found two of them — one asserting a number the
  demo only produced because its clock ran backwards.
* Never `pip install` into the shared environment to make something work. It is not
  yours, and a session has already broken a working install that way.
* The analysis modules (`sacct`, `model`, `diagnose`, `patterns`, `nodes`, `index`,
  `sizing`, `logs`, `site`, `duration`) import no third-party package — keep it that
  way. Only `render`, `report` and `tui` may touch `rich`/`textual`.
* `render.py` exists so the dashboard and `--plain` cannot drift. A change to one
  surface that the other also draws belongs in `render.py`, not in both.
